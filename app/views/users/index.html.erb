<h1>Users</h1>

<%= link_to 'Add user', new_user_path %>
<table>
  <% @users.each do |u| %>
    <tr>
      <td><a href="<%= user_path(u) %>"><%= u.name %></a></td>
      <td><%= link_to 'Edit', edit_user_path(u) %></td>
      <td><%= link_to 'Delete', user_path(u), method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
</table>

